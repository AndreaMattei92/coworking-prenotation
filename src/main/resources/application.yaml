server:
  port: 8090

spring:
  keycloak:
    auth-server-url: http://localhost:8080/auth
    realm: spring-realm
    resource: coworking-prenotation
    credentials:
      secret: fE7I437LZgMWXm56ClBFNFaRd9mXmyqe

  datasource:
    url: jdbc:postgresql://localhost:5432/coworkingdb
    driver-class-name: org.postgresql.Driver
    username: admin
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org.springframework: INFO

security:
  oauth2:
    resourceserver:
      jwt:
        issuer-uri: http://localhost:8080/realms/spring-realm
